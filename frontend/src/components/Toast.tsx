import React from "react";

const Toast: React.FC = () => {
  return (
    <div className="log">
      <div>
        <div className="log_text">Amount updated successfully.</div>
      </div>
    </div>
  );
};

export default Toast;
